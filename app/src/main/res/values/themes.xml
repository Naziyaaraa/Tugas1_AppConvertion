<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NaziaApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.NaziaApp" parent="Base.Theme.NaziaApp" />
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/putih</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/zy</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.NaziaApp</item>
    </style>
</resources>